# Shared (共享组件)

平台和 Agent 共享的组件和协议定义。

## 目录结构

```
shared/
├── proto/                 # 通信协议定义
├── pkg/                   # 共享工具包
└── config/                # 共享配置结构
```

## 用途

- 定义 Agent 与 Platform 之间的通信协议
- 共享工具函数和常量
- 统一配置结构定义
