---
description: 用户侧开发焦点、任务优先级与 TASK_BOARD 同步义务
alwaysApply: true
---

# 工作进度与任务来源

## 当前焦点

- 优先完成 **用户侧（`agent/`）** 开发与交付；平台侧任务不主动认领，除非用户侧被外部依赖阻塞需协作说明。
- 目录与迁移边界以 `docs/architecture/DIRECTORY_MIGRATION.md` 为准（新代码落在 `agent/` 等，勿在根目录 `src/`、`config/` 等旧结构上扩展）。

## 用户侧任务执行顺序（与 `docs/TASK_BOARD.md` 一致）

当前 **P0（Phase 1）已完成**，按 **P1（Phase 2）** 顺序推进，且以任务编号顺序为准：

1. **AGENT-007**：脚本支持安装 OpenClaw（官方渠道 latest + 轻预装层 + exporter health 打通）— ✅ 已完成
2. **AGENT-008**：OpenClaw 开箱即用配置自动化（可选 `openclaw onboard --non-interactive`）— ✅ 已完成
3. **AGENT-009（P1 当前）**：调整 Python import 路径
4. **AGENT-010**：实现 selfhealer/l1_local_fix.py
5. **AGENT-011**：实现 updater/update_client.py
6. **AGENT-012**：创建 platform/docker-compose.yml 完整版

其余 **P2/P3** 任务在 P1 关键路径完成后推进，或按用户明确要求调整。

## 维护 `docs/TASK_BOARD.md`（强制）

- **每完成一项可验收的用户侧任务**（代码/文档产出符合该任务「产出」描述）：在同一提交或紧随其后的提交中更新 `docs/TASK_BOARD.md`。
- 更新内容至少包括：对应条目的 **状态**（如 ✅ 已完成）、**最后更新**日期（文件顶部）、必要时 **任务变更日志** 一条、**任务统计** 表数字、**最近完成任务** 列表。
- 若任务 **阻塞**：将状态改为阻塞、在「阻塞问题」表补充或更新行，并写明依赖（例如等待 PLAT-003）。

## 维护 `docs/architecture/PREINSTALLED_LOBSTER_ROADMAP.md`（强制）

- AGENT-007 相关工作必须同步维护 `docs/architecture/PREINSTALLED_LOBSTER_ROADMAP.md`。
- `docs/architecture/PREINSTALLED_LOBSTER_ROADMAP.md` 记录 **AGENT-007 的具体执行信息与细粒度检查项**（分期内 checklist、约束、判定规则）。
- `docs/TASK_BOARD.md` 记录 **AGENT-007 的里程碑与节点状态**（状态流转、负责人、重要节点、统计与最近完成）。
- 两者不得混用：Roadmap 管细节，Task Board 管里程碑与全局进度。

## 示例：完成 AGENT-001 后 TASK_BOARD 应出现的变化

```markdown
#### AGENT-001: 设计安装脚本方案
- **状态**: ✅ 已完成
```

并在「📝 任务变更日志」增加当日条目，在「✅ 最近完成任务」列出 AGENT-001。
