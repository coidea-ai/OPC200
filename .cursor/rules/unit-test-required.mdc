---
description: 代码任务完成后必须编写单元测试
alwaysApply: true
---

# 单元测试要求

每完成一项产出代码的任务（新增模块、功能函数、bug 修复），必须同步编写对应的单元测试。

## 规则

- 测试文件放在 `tests/agent/`（Agent 侧）或 `tests/platform/`（平台侧），文件名 `test_<模块名>.py`。
- 使用 `pytest`；mock 外部依赖（网络、文件系统、硬件接口）。
- 覆盖正常路径和至少一个异常/边界场景。
- 提交 commit 前确保 `pytest <测试文件>` 通过。
