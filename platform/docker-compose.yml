version: '3.8'

# OPC200 平台侧监控栈
# Push 架构：接收用户端推送的指标 + 告警通知

services:
  # Pushgateway - 接收所有 Agent 推送的指标
  pushgateway:
    image: prom/pushgateway:v1.7.0
    container_name: opc200-pushgateway
    ports:
      - "9091:9091"
    command:
      - --persistence.file=/data/metrics
      - --persistence.interval=5m
    volumes:
      - pushgateway-data:/data
    networks:
      - opc200-platform
    restart: unless-stopped

  # Prometheus - 统一时序存储 + 告警规则评估
  prometheus:
    image: prom/prometheus:v2.49.1
    container_name: opc200-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/rules:/etc/prometheus/rules:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'
    networks:
      - opc200-platform
    restart: unless-stopped
    depends_on:
      - pushgateway
      - alertmanager

  # Alertmanager - 告警路由和通知
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: opc200-alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager-data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=http://100.74.18.112:9093'
    networks:
      - opc200-platform
    restart: unless-stopped

  # Grafana - 可视化仪表盘
  grafana:
    image: grafana/grafana:10.3.1
    container_name: opc200-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=opc200admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3000
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    networks:
      - opc200-platform
    restart: unless-stopped
    depends_on:
      - prometheus

volumes:
  pushgateway-data:
  prometheus-data:
  grafana-data:
  alertmanager-data:

networks:
  opc200-platform:
    driver: bridge
